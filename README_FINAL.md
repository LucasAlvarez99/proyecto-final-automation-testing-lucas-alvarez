# proyecto-final-automation-testing-lucas-alvarez

## Propósito
Framework de automatización que cubre pruebas de UI (Selenium + POM) y pruebas de API (requests). Genera reportes HTML y capturas automáticas.

## Estructura
(Ver estructura en el repo)

## Requisitos
- Python 3.8+
- Chrome
- Opcional: chromedriver (si no usás webdriver-manager)

## Instalación
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

pip install -r requirements.txt
